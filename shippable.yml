language: none

services:
   #- mysql
   #- selenium
   #- postgres
   #- redis
   #- rabbitmq
   #- mongodb
   #- memcached
   #- riak
   #- elasticsearch
   #- couchdb
   - neo4j
   - cassandra
  
   

build:

  #pre_ci:
   #- git clone https://github.com/vijayreddy1991/u14all.git
   #- cd u14all
   #- docker build -t drydock/u14alltest:latest .

  pre_ci_boot:
    image_name: drydock/u14javall
    image_tag: master
    pull: true
    
    
  ci:
    - mysql --version
    - java -version
    - printenv
    - sqlite3 --version
    - psql --version
    - redis-server --version
    - dpkg -s rabbitmq-server | grep Version
    - mongod --version
    - memcached --version
    - riak version
    - elasticsearch --version
    - sleep 5
    - curl http://127.0.0.1:5984
    - cassandra -v
    - pip show pycrypto
    - /var/lib/neo4j/bin/neo4j version
    
